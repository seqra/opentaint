rules:
  - id: example-NegOnlyNotInsideDoc
    languages:
      - java
    severity: ERROR
    message: match example/NegOnlyNotInsideDoc
    patterns:
      - pattern: $CLIENT.connect($URL)
      - pattern-not-inside: |
          $FACTORY.configure($MODE);
          ...
