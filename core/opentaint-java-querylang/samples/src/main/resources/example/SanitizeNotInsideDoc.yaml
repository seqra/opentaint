rules:
  - id: example-SanitizeNotInsideDoc
    languages:
      - java
    severity: ERROR
    message: match example/SanitizeNotInsideDoc
    patterns:
      - pattern: |
          $RESULT = decode($INPUT);
          ...
          consume($RESULT);
      - pattern-not-inside: |
          $RESULT = sanitize($RESULT);
          ...
