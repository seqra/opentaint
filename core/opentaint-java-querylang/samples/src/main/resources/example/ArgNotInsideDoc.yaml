rules:
  - id: example-ArgNotInsideDoc
    languages:
      - java
    severity: ERROR
    message: match example/ArgNotInsideDoc
    patterns:
      - pattern: |
          $RESULT = decode($INPUT);
          ...
          consume($RESULT);
      - pattern-not-inside: |
          check($RESULT);
          ...
