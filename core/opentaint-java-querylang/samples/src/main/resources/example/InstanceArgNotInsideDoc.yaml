rules:
  - id: example-InstanceArgNotInsideDoc
    languages:
      - java
    severity: ERROR
    message: match example/InstanceArgNotInsideDoc
    patterns:
      - pattern-inside: |
          $RESULT = decode($INPUT);
          ...
      - pattern: consume($RESULT)
      - pattern-not-inside: |
          $_.verify($RESULT);
          ...
