rules:
  - id: phase3-coverage-sql
    languages:
      - java
    severity: ERROR
    message: taint reaches sink through a javax.sql.rowset passthrough
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: $X = rsrc();
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern: objSink($Y);
          - focus-metavariable: $Y
