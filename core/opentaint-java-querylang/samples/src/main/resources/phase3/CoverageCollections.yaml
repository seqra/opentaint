rules:
  - id: phase3-coverage-collections
    languages:
      - java
    severity: ERROR
    message: taint reaches sink through an immutable-factory element passthrough
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: $X = ssrc();
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern: strSink($Y);
          - focus-metavariable: $Y
