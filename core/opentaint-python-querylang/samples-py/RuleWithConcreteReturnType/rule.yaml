rules:
  - id: example-rulewithconcretereturtype
    languages: [python]
    severity: ERROR
    message: match example/RuleWithConcreteReturnType (Python port)
    patterns:
      - pattern: sink($A)
      - pattern-inside: |
          def $METHOD(..., $A: str, ...) -> str:
              ...
