rules:
  - id: example-rulewithpatternssimple
    languages: [python]
    severity: ERROR
    message: match example/RuleWithPatternsSimple (Python port)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
