rules:
  - id: example-rulewitharrayofparameterized
    languages: [python]
    severity: ERROR
    message: match example/RuleWithArrayOfParameterized (Python port)
    patterns:
      - pattern: sink($A)
      - pattern-inside: |
          def $METHOD(..., $A: str, ...) -> List[List[str]]:
              ...
