rules:
  - id: example-rulewithanypattern
    languages: [python]
    severity: ERROR
    message: match example/RuleWithAnyPattern (Python port)
    patterns:
      - pattern: method($A)
      - pattern-inside: |
          def $METHOD(..., $A: str, ...) -> $RET:
              ...
