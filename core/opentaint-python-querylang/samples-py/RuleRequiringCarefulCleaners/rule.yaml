rules:
  - id: example-rulerequiringcarefulcleaners
    languages: [python]
    severity: ERROR
    message: match example/RuleRequiringCarefulCleaners (Python port)
    mode: taint
    pattern-sources:
      - pattern: get_obj_good($X)
    pattern-sanitizers:
      - pattern: get_obj_bad($X)
    pattern-sinks:
      - pattern: sink($X)
