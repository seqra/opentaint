rules:
  - id: example-r1
    languages: [python]
    severity: ERROR
    message: match example/R1 (Python port)
    patterns:
      - pattern: |-
          ...
          return $X
      - pattern-inside: |-
          $X = foo($ARG)
          return $X
      - pattern-not-inside: |-
          def $FUN($ARG):
              $X = foo($ARG)
              ...
