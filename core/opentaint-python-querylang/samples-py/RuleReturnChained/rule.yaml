rules:
  - id: example-rulereturnchained
    languages: [python]
    severity: ERROR
    message: match example/RuleReturnChained (Python port, chained calls)
    mode: taint
    pattern-sources:
      - pattern: mk_type2($X)
    pattern-sinks:
      - pattern: mk_type1($X)
