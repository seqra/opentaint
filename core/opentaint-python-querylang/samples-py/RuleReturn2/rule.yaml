rules:
  - id: example-rulereturn2
    languages: [python]
    severity: ERROR
    message: match example/RuleReturn2 (Python port, return with cleaner)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sanitizers:
      - pattern: clean($X)
    pattern-sinks:
      - pattern: sink($X)
