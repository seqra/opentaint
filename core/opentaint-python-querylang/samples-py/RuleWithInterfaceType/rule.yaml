rules:
  - id: example-rulewithinterfacetype
    languages: [python]
    severity: ERROR
    message: match example/RuleWithInterfaceType (Python port, distinct foo per receiver type)
    mode: taint
    pattern-sources:
      - pattern: make_simple()
    pattern-sinks:
      - pattern: foo_simple($X)
