rules:
  - id: example-rulewithdeepnesting
    languages: [python]
    severity: ERROR
    message: match example/RuleWithDeepNesting (Python port)
    patterns:
      - pattern: sink($A)
      - pattern-inside: |
          def $METHOD(..., $A: str, ...) -> List[List[str]]:
              ...
