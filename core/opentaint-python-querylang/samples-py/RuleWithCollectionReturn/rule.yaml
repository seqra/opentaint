rules:
  - id: example-rulewithcollectionreturn
    languages: [python]
    severity: ERROR
    message: match example/RuleWithCollectionReturn (Python port)
    patterns:
      - pattern: sink($A)
      - pattern-inside: |
          def $METHOD(..., $A: str, ...) -> List[str]:
              ...
