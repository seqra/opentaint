rules:
  - id: example-rulewithtwodimarray
    languages: [python]
    severity: ERROR
    message: match example/RuleWithTwoDimArrayReturn (Python port)
    patterns:
      - pattern: sink($A)
      - pattern-inside: |
          def $METHOD(..., $A: str, ...) -> List[List[str]]:
              ...
