rules:
  - id: example-rulereturninsidesignature2
    languages: [python]
    severity: ERROR
    message: match example/RuleReturnInsideSignature2 (Python port)
    mode: taint
    pattern-sources:
      - pattern: pass_through($X)
    pattern-sinks:
      - pattern: sink($X)
