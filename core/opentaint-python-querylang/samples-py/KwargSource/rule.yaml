rules:
  - id: kwarg-source
    languages: [python]
    severity: ERROR
    message: "The payload keyword argument of read() is a taint source"
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: read(payload=$X)
          - focus-metavariable: $X
    pattern-sinks:
      - pattern: sink($Y)
