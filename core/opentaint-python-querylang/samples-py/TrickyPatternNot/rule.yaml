rules:
  - id: example-trickypatternnot
    languages: [python]
    severity: ERROR
    message: match example/TrickyPatternNot (Python port, return with cleaner)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sanitizers:
      - pattern: clean($X)
    pattern-sinks:
      - pattern: sink($X)
