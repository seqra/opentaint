rules:
  - id: example-rulereturnwithnotinsidesignature
    languages: [python]
    severity: ERROR
    message: match example/RuleReturnWithNotInsideSignature (Python port, return with cleaner)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sanitizers:
      - pattern: clean($X)
    pattern-sinks:
      - pattern: sink($X)
