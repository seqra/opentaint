rules:
  - id: example-rulereturnwithnotinsidesignature
    languages: [python]
    severity: ERROR
    message: match example/RuleReturnWithNotInsideSignature (Python port)
    patterns:
      - pattern: |
          @entry_point
          def $METHOD($PARAM):
              ...
              return $PARAM
      - pattern-not-inside: |
          @entry_point
          def $METHOD($PARAM):
              ...
              return clean($PARAM)
