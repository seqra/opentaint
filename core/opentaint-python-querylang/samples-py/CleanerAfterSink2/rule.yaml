rules:
  - id: example-cleaneraftersink2
    languages: [python]
    severity: ERROR
    message: match example/CleanerAfterSink2 (Python port, two-arg sink/clean)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sanitizers:
      - pattern: clean($X, $Y)
    pattern-sinks:
      - pattern: sink($X, $Y)
