rules:
  - id: example-rulewithgenerictypeargs
    languages: [python]
    severity: ERROR
    message: match example/RuleWithGenericTypeArgs (Python port)
    patterns:
      - pattern: sink($A)
      - pattern-inside: |
          def $METHOD($M: Dict[str, object], ...) -> $RET:
              ...
