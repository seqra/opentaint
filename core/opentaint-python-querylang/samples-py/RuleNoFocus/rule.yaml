rules:
  - id: taint-rule-no-focus
    languages: [python]
    severity: ERROR
    message: match taint/RuleNoFocus (Python port)
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: $X = src()
    pattern-sinks:
      - patterns:
          - pattern: sink($Y)
