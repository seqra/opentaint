rules:
  - id: example-rulewithstaticfield
    languages: [python]
    severity: ERROR
    message: match example/RuleWithStaticField (Python port, discriminating class attribute)
    mode: taint
    pattern-sources:
      - pattern: StaticConstantStorage.FIRST
    pattern-sinks:
      - pattern: sink($X)
