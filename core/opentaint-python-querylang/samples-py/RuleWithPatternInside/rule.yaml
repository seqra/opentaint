rules:
  - id: example-rulewithpatterninside
    languages: [python]
    severity: ERROR
    message: match example/RuleWithPatternInside (Python port)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
