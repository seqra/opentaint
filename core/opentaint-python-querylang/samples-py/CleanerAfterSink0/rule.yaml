rules:
  - id: example-cleaneraftersink0
    languages: [python]
    severity: ERROR
    message: match example/CleanerAfterSink0 (Python port)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sanitizers:
      - pattern: clean($X)
    pattern-sinks:
      - pattern: sink($X)
