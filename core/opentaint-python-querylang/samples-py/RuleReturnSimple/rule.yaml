rules:
  - id: example-rulereturnsimple
    languages: [python]
    severity: ERROR
    message: match example/RuleReturnSimple (Python port)
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
