rules:
  - id: py-kwarg-structural
    languages: [python]
    severity: ERROR
    message: "structural kwarg constant sink"
    patterns:
      - pattern: |
          $A = source()
          sink($A, mode="constant")
