rules:
  - id: owasp-securecookie-01207
    languages: [python]
    severity: ERROR
    message: "Cookie set without the Secure flag (CWE-614)"
    metadata:
      cwe: CWE-614
    patterns:
      - pattern: $RESP.set_cookie(..., secure=False, ...)
