rules:
  - id: owasp-redirect-01158
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches flask.redirect (open redirect / CWE-601)"
    metadata:
      cwe: CWE-601
    patterns:
      - pattern: |
          $A = flask.request.form.getlist(...)
          ...
          flask.redirect($URL)
