rules:
  - id: owasp-xxe-00462
    languages: [python]
    mode: taint
    message: "Tainted request data reaches an external-entity-enabled XML parse (XXE)"
    severity: ERROR
    metadata:
      cwe: CWE-611
    pattern-sources:
      - pattern: "flask.request.headers.get(...)"
    pattern-sinks:
      - patterns:
          - pattern-inside: |
              $P.setFeature($F, True)
              ...
          - pattern: "xml.dom.minidom.parseString($DOC, $P)"
          - focus-metavariable: $DOC
