rules:
  - id: owasp-codeinj-00820
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches eval/exec (code injection / CWE-94)"
    metadata:
      cwe: CWE-94
    patterns:
      - pattern: |
          $A = flask.request.args.getlist(...)[...]
          ...
          exec($A)
