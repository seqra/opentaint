rules:
  - id: owasp-deserialization-00514
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches yaml.load (unsafe deserialization / CWE-502)"
    metadata:
      cwe: CWE-502
    patterns:
      - pattern: |
          $A = flask.request.headers.get(...)
          ...
          yaml.load($A, ...)
