rules:
  - id: owasp-xpathi-00303
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches an XPath query (XPath injection / CWE-643)"
    metadata:
      cwe: CWE-643
    patterns:
      - pattern: |
          $A = flask.request.form.get(...)
          ...
          $T.xpath($A)
