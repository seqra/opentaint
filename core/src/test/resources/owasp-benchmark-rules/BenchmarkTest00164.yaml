rules:
  - id: owasp-deserialization-00164
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches pickle.loads (unsafe deserialization / CWE-502)"
    metadata:
      cwe: CWE-502
    patterns:
      - pattern: |
          $A = flask.request.form.get(...)
          ...
          pickle.loads($A)
