rules:
  - id: owasp-deserialization-00270
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches yaml.load (unsafe deserialization / CWE-502)"
    metadata:
      cwe: CWE-502
    patterns:
      - pattern: |
          $A = flask.request.form.getlist(...)[...]
          ...
          yaml.load($A, ...)
