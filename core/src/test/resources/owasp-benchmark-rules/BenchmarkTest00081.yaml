rules:
  - id: owasp-deserialization-00081
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches yaml.load (unsafe deserialization / CWE-502)"
    metadata:
      cwe: CWE-502
    patterns:
      - pattern: |
          $A = flask.request.cookies.get(...)
          ...
          yaml.load($A, ...)
