rules:
  - id: owasp-codeinj-00890
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches eval (code injection / CWE-94)"
    metadata:
      cwe: CWE-94
    patterns:
      - pattern: |
          $A = flask.request.args.get(...)
          ...
          eval($A)
