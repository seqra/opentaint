rules:
  - id: owasp-ldapi-00076
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches an LDAP search filter (LDAP injection)"
    metadata:
      cwe: CWE-90
    patterns:
      - pattern: |
          $FILTER = flask.request.cookies.get(...)
          ...
          $CONN.search($BASE, $FILTER, ...)
