rules:
  - id: owasp-sqli-01203
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches a SQL execute (SQL injection)"
    metadata:
      cwe: CWE-89
    patterns:
      - pattern: |
          $Q = flask.request.args.get(...)
          ...
          $DB.execute($Q, ...)
