rules:
  - id: owasp-sqli-01229
    languages: [python]
    mode: taint
    message: "Tainted request data reaches a SQL execute (SQL injection)"
    severity: ERROR
    metadata:
      cwe: CWE-89
    pattern-sources:
      - pattern: "flask.request"
    pattern-sinks:
      - patterns:
          - pattern: "$DB.execute($Q, ...)"
          - focus-metavariable: $Q
