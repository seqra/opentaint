rules:
  - id: owasp-codeinj-00599
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches eval/exec (code injection / CWE-94)"
    metadata:
      cwe: CWE-94
    patterns:
      - pattern: |
          $A = flask.request.headers.getlist(...)[...]
          ...
          eval($A)
