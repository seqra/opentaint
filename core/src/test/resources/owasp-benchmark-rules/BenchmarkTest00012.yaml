rules:
  - id: owasp-sqli-00012
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches a SQL execute (SQL injection)"
    metadata:
      cwe: CWE-89
    patterns:
      - pattern: |
          $Q = flask.request.cookies.get(...)
          ...
          $DB.execute($Q, ...)
