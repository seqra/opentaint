rules:
  - id: owasp-deserialization-01096
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches pickle.loads (unsafe deserialization / CWE-502)"
    metadata:
      cwe: CWE-502
    patterns:
      - pattern: |
          $A = flask.request.args.getlist(...)[...]
          ...
          pickle.loads($A)
