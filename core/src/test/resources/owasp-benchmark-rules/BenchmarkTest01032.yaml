rules:
  - id: owasp-xpathi-01032
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches an XPath query (XPath injection / CWE-643)"
    metadata:
      cwe: CWE-643
    patterns:
      - pattern: |
          $A = flask.request.query_string
          ...
          $T.xpath($A)
