rules:
  - id: owasp-ldapi-01095
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches an LDAP search filter (LDAP injection)"
    metadata:
      cwe: CWE-90
    patterns:
      - pattern: |
          $FILTER = flask.request.form.get(...)
          ...
          $CONN.search($BASE, $FILTER, ...)
