rules:
  - id: owasp-ldapi-00823
    languages: [python]
    severity: ERROR
    message: "Tainted request data reaches an LDAP search filter (LDAP injection)"
    metadata:
      cwe: CWE-90
    patterns:
      - pattern-either:
          - pattern: |
              $FILTER = flask.request.args.getlist(...)
              ...
              $CONN.search($BASE, $FILTER, ...)
          - pattern: |
              $FILTER = flask.request.args.getlist(...)[...]
              ...
              $CONN.search($BASE, $FILTER, ...)
