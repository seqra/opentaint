rules:
  - id: java-rule-return-simple
    languages: [go]
    severity: ERROR
    message: match example/RuleReturnSimple (Go port, recast as call-based)
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
