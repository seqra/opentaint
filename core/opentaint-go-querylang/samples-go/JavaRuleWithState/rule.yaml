rules:
  - id: java-rule-with-state
    languages: [go]
    severity: ERROR
    message: match example/RuleWithState (Go port — sequence via state token)
    mode: taint
    pattern-sources:
      - pattern: "util.F()"
    pattern-sinks:
      - pattern: "util.G($X)"
