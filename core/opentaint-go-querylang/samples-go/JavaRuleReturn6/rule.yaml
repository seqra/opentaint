rules:
  - id: java-rule-return-6
    languages: [go]
    severity: ERROR
    message: match example/RuleReturn6 (Go port — source-without-sink semantics)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sinks:
      - pattern: "util.Sink($X)"
