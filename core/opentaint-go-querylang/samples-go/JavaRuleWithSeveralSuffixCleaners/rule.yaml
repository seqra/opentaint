rules:
  - id: java-rule-with-several-suffix-cleaners
    languages: [go]
    severity: ERROR
    message: match example/RuleWithSeveralSuffixCleaners (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sanitizers:
      - pattern: "util.Clean1($X)"
      - pattern: "util.Clean2($X)"
    pattern-sinks:
      - pattern: "util.F($X)"
