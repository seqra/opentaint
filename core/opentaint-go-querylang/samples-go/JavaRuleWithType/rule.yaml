rules:
  - id: java-rule-with-type
    languages: [go]
    severity: ERROR
    message: match example/RuleWithType (Go port, distinct foos per receiver)
    mode: taint
    pattern-sources:
      - pattern: "util.MakeSimple()"
    pattern-sinks:
      - pattern: "util.FooSimple($X)"
