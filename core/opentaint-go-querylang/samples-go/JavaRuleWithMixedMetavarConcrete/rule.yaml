rules:
  - id: java-rule-with-mixed-metavar-concrete
    languages: [go]
    severity: ERROR
    message: match example/RuleWithMixedMetavarConcrete (Go port)
    mode: taint
    pattern-sources:
      - pattern: "JavaRuleWithMixedMetavarConcrete.Source()"
    pattern-sinks:
      - pattern: "JavaRuleWithMixedMetavarConcrete.Sink($X)"
