rules:
  - id: java-rule-return-not-inside-prefix
    languages: [go]
    severity: ERROR
    message: match example/RuleReturnNotInsidePrefix (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.MkType2($X)"
    pattern-sanitizers:
      - pattern: "util.Clean($X)"
    pattern-sinks:
      - pattern: "util.MkType1($X)"
