rules:
  - id: source-form-parse
    languages: [go]
    severity: WARNING
    message: "Taint from r.Form reaches Sink"
    mode: search
    pattern: |
      $X = MatchingRuleWithSink.Source()
      ...
      MatchingRuleWithSink.Sink($X)
