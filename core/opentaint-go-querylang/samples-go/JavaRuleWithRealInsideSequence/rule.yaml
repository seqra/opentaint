rules:
  - id: java-rule-with-real-inside-sequence
    languages: [go]
    severity: ERROR
    message: match example/RuleWithRealInsideSequence (Go port — ObjectMapper-like sequence)
    mode: taint
    pattern-sources:
      - pattern: "util.EnableDefaultTyping($OM)"
    pattern-sinks:
      - pattern: "util.ReadValue($X)"
