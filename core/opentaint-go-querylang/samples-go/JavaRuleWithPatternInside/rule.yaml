rules:
  - id: java-rule-with-pattern-inside
    languages: [go]
    severity: ERROR
    message: match example/RuleWithPatternInside (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
