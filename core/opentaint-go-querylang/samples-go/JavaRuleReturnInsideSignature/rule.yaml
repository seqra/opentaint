rules:
  - id: java-rule-return-inside-signature
    languages: [go]
    severity: ERROR
    message: match example/RuleReturnInsideSignature (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Src($X)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
