rules:
  - id: java-rule-with-not-inside-prefix
    languages: [go]
    severity: ERROR
    message: match example/RuleWithNotInsidePrefix (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sanitizers:
      - pattern: "util.PrefixClean($X)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
