rules:
  - id: java-cleaner-after-sink-1
    languages: [go]
    severity: ERROR
    message: match example/CleanerAfterSink1 (Go port)
    mode: taint
    pattern-sources:
      - pattern: "JavaCleanerAfterSink1.Source(...)"
    pattern-sanitizers:
      - pattern: "JavaCleanerAfterSink1.Clean($X)"
    pattern-sinks:
      - pattern: "JavaCleanerAfterSink1.Sink($X)"
