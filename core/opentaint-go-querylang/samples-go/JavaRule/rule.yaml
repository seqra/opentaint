rules:
  - id: java-rule
    languages: [go]
    severity: ERROR
    message: match example/Rule (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sanitizers:
      - pattern: "util.Clean($X)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
