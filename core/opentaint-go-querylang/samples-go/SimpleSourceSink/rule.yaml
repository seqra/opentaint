rules:
  - id: simple-source-sink
    languages: [go]
    severity: WARNING
    message: "Taint from util.Source reaches util.Sink"
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
