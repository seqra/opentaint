rules:
  - id: java-nd-rule
    languages: [go]
    severity: ERROR
    message: match example/NDRule (Go port, two sources joined via pass)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sinks:
      - pattern: "util.Sink($X)"
