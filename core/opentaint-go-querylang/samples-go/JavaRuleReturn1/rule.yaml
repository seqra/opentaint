rules:
  - id: java-rule-return-1
    languages: [go]
    severity: ERROR
    message: match example/RuleReturn1 (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sinks:
      - pattern: "util.Sink($X)"
