rules:
  - id: java-rule-return-not-inside
    languages: [go]
    severity: ERROR
    message: match example/RuleReturnNotInside (Go port, recast as call-based)
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sanitizers:
      - pattern: "util.Sanitize($X)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
