rules:
  - id: nil-check
    languages: [go]
    severity: WARNING
    message: "Taint reaches Sink without a nil check"
    mode: taint
    pattern-sources:
      - pattern: "util.Source(...)"
    pattern-sanitizers:
      - pattern: "util.IsNil($X)"
        by-side-effect: true
    pattern-sinks:
      - pattern: "util.Sink($X)"
