rules:
  - id: java-r2
    languages: [go]
    severity: ERROR
    message: match example/R2 (Go port, recast as source-sink return)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sinks:
      - pattern: "util.Sink($X)"
