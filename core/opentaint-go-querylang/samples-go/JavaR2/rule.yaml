rules:
  - id: java-r2
    languages: [go]
    severity: ERROR
    message: match example/R2 (Go port, recast as source-sink return)
    mode: taint
    pattern-sources:
      - pattern: "JavaR2.Source()"
    pattern-sinks:
      - pattern: "JavaR2.Sink($X)"
