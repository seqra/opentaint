rules:
  - id: java-cleaner-after-sink-2
    languages: [go]
    severity: ERROR
    message: match example/CleanerAfterSink2 (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sanitizers:
      - pattern: "util.Clean($X, $Y)"
    pattern-sinks:
      - pattern: "util.Sink($X, $Y)"
