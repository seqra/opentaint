rules:
  - id: java-cleaner-after-sink-0
    languages: [go]
    severity: ERROR
    message: match example/CleanerAfterSink0 (Go port)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sanitizers:
      - pattern: "util.Clean($X)"
    pattern-sinks:
      - pattern: "util.Sink($X)"
