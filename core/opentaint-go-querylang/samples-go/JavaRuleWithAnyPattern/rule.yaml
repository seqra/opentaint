rules:
  - id: java-rule-with-any-pattern
    languages: [go]
    severity: ERROR
    message: match example/RuleWithAnyPattern (Go port, recast)
    mode: taint
    pattern-sources:
      - pattern: "util.Source()"
    pattern-sinks:
      - pattern: "util.Method($X)"
