rules:
  - id: sqlinj-09-args-switch
    languages: [go]
    severity: WARNING
    message: Tainted os.Args via switch reaches db.Exec (SQL injection)
    mode: taint
    pattern-sources:
      - pattern: os.Args[$IDX]
    pattern-sinks:
      - pattern: util.Sink_DbExec($X)
