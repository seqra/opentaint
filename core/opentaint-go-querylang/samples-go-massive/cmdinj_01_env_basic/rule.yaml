rules:
  - id: cmdinj-01-env-basic
    languages: [go]
    severity: WARNING
    message: Tainted env reaches exec.Command (command injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("CMD")
    pattern-sinks:
      - pattern: util.Sink_ExecCommand($X)
