rules:
  - id: cmdinj-08-env-if-branch
    languages: [go]
    severity: WARNING
    message: Tainted env in one branch reaches exec.Command (command injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("ACTION")
    pattern-sinks:
      - pattern: util.Sink_ExecCommand($X)
