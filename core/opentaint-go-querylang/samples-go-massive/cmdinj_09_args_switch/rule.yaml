rules:
  - id: cmdinj-09-args-switch
    languages: [go]
    severity: WARNING
    message: Tainted os.Args via switch reaches bash -c (command injection)
    mode: taint
    pattern-sources:
      - pattern: os.Args[$IDX]
    pattern-sinks:
      - pattern: util.Sink_ExecBash($X)
