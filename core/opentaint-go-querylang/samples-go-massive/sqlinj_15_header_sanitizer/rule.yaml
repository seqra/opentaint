rules:
  - id: sqlinj-15-header-sanitizer
    languages: [go]
    severity: WARNING
    message: Tainted header reaches db.Query unless sanitized (SQL injection)
    mode: taint
    pattern-sources:
      - pattern: $R.Header.Get($K)
    pattern-sinks:
      - pattern: util.Sink_DbQuery($X)
    pattern-sanitizers:
      - pattern: strings.ReplaceAll($V, ";", "")
