rules:
  - id: sqlinj-20-env-full-chain
    languages: [go]
    severity: WARNING
    message: Tainted env through long chain reaches db.Query (SQL injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("Q")
    pattern-sinks:
      - pattern: util.Sink_DbQuery($X)
