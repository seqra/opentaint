rules:
  - id: cmdinj-05-header-trim
    languages: [go]
    severity: WARNING
    message: Tainted HTTP header reaches exec.LookPath (command injection)
    mode: taint
    pattern-sources:
      - pattern: $R.Header.Get($K)
    pattern-sinks:
      - pattern: util.Sink_ExecLookPath($X)
