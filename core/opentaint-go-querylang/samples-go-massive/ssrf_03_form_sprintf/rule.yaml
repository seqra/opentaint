rules:
  - id: ssrf-03-form-sprintf
    languages: [go]
    severity: WARNING
    message: Tainted form value flows via fmt.Sprintf URL into http.Get (SSRF)
    mode: taint
    pattern-sources:
      - pattern: $R.FormValue($K)
    pattern-sinks:
      - pattern: http.Get($U)
