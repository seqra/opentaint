rules:
  - id: hdrinj-06-query-redirect-status
    languages: [go]
    severity: WARNING
    message: Tainted URL query lowercased and passed to http.Redirect URL (header injection)
    mode: taint
    pattern-sources:
      - pattern: $R.URL.Query().Get($K)
    pattern-sinks:
      - patterns:
          - pattern: http.Redirect($W, $R, $URL, $CODE)
          - focus-metavariable: $URL
