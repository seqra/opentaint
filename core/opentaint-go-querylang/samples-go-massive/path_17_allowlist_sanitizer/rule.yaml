rules:
  - id: path-17-allowlist-sanitizer
    languages: [go]
    severity: WARNING
    message: Tainted form value reaches os.Open unless run through util.allowlist (filepath.Clean is NOT sufficient)
    mode: taint
    pattern-sources:
      - pattern: $R.FormValue($K)
    pattern-sinks:
      - pattern: os.Open($P)
    pattern-sanitizers:
      - pattern: util.allowlist($X)
