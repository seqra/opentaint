rules:
  - id: loginj-05-query-logoutput
    languages: [go]
    severity: WARNING
    message: Tainted URL query reaches log.Output $MSG (log injection)
    mode: taint
    pattern-sources:
      - pattern: $R.URL.Query().Get($K)
    pattern-sinks:
      - patterns:
          - pattern: $L.Output($DEPTH, $MSG)
          - focus-metavariable: $MSG
