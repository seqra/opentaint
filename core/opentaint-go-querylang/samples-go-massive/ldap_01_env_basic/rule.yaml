rules:
  - id: ldap-01-env-basic
    languages: [go]
    severity: WARNING
    message: Tainted env reaches LDAP search filter (LDAP injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("LDAP_UID")
    pattern-sinks:
      - pattern: util.Sink_LdapSearch($X)
