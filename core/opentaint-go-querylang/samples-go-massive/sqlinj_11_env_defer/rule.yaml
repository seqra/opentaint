rules:
  - id: sqlinj-11-env-defer
    languages: [go]
    severity: WARNING
    message: Tainted env via defer reaches db.Exec (SQL injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("CLEANUP")
    pattern-sinks:
      - pattern: util.Sink_DbExec($X)
