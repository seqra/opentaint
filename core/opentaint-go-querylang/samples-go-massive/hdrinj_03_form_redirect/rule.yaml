rules:
  - id: hdrinj-03-form-redirect
    languages: [go]
    severity: WARNING
    message: Tainted FormValue reaches http.Redirect URL (header injection)
    mode: taint
    pattern-sources:
      - pattern: $R.FormValue($K)
    pattern-sinks:
      - patterns:
          - pattern: http.Redirect($W, $R, $URL, $CODE)
          - focus-metavariable: $URL
