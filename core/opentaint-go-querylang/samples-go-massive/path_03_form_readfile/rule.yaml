rules:
  - id: path-03-form-readfile
    languages: [go]
    severity: WARNING
    message: Tainted form value reaches os.ReadFile (path traversal)
    mode: taint
    pattern-sources:
      - pattern: $R.FormValue($K)
    pattern-sinks:
      - pattern: os.ReadFile($P)
