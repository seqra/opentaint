rules:
  - id: crypto-11-query-branch-sha1
    languages: [go]
    severity: WARNING
    message: Tainted query reaches sha1 hash.Write through branch
    mode: taint
    pattern-sources:
      - pattern: $R.URL.Query().Get($K)
    pattern-sinks:
      - pattern: $H.Write($X)
