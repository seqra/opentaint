rules:
  - id: hdrinj-05-query-redirect
    languages: [go]
    severity: WARNING
    message: Tainted URL query reaches http.Redirect URL (header injection)
    mode: taint
    pattern-sources:
      - pattern: $R.URL.Query().Get($K)
    pattern-sinks:
      - patterns:
          - pattern: http.Redirect($W, $R, $URL, $CODE)
          - focus-metavariable: $URL
