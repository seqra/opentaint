rules:
  - id: ssrf-02-query-get
    languages: [go]
    severity: WARNING
    message: Tainted URL query param reaches http.Get (classic SSRF)
    mode: taint
    pattern-sources:
      - pattern: $R.URL.Query().Get($K)
    pattern-sinks:
      - pattern: http.Get($U)
