rules:
  - id: cmdinj-06-env-helper1
    languages: [go]
    severity: WARNING
    message: Tainted env via helper reaches exec.Command (command injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("TOOL")
    pattern-sinks:
      - pattern: util.Sink_ExecCommand($X)
