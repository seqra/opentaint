rules:
  - id: nosql-01-env-basic
    languages: [go]
    severity: WARNING
    message: Tainted env reaches MongoDB Find filter (NoSQL injection)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv("MONGO_USER")
    pattern-sinks:
      - pattern: util.Sink_MongoFind($X)
