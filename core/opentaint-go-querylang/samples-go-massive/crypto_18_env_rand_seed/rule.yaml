rules:
  - id: crypto-18-env-rand-seed
    languages: [go]
    severity: WARNING
    message: Tainted env reaches predictable RNG seed (math/rand seed sink)
    mode: taint
    pattern-sources:
      - pattern: os.Getenv($X)
    pattern-sinks:
      - pattern: util.Sink_RandSeed($X)
