rules:
  - id: xss-20-variadic-sprintf
    languages: [go]
    severity: WARNING
    message: Tainted form value passes through variadic fmt.Sprintf to w.Write (reflected XSS)
    mode: taint
    pattern-sources:
      - pattern: $R.FormValue($K)
    pattern-sinks:
      - pattern: $W.Write($B)
