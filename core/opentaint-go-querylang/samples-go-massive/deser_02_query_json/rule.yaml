rules:
  - id: deser-02-query-json
    languages: [go]
    severity: WARNING
    message: Tainted URL query reaches json.Unmarshal (deserialization)
    mode: taint
    pattern-sources:
      - pattern: $R.URL.Query().Get($K)
    pattern-sinks:
      - pattern: json.Unmarshal($DATA, $TARGET)
