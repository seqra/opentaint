This directory intentionally contains a tracked placeholder so `go build ./...`
works before `go generate` creates `opentaint-sast-test-util.jar`.

Generated JAR files in this directory are ignored by Git.
