rules:
  - id: generic-taint-source
    options:
      lib: true
    severity: NOTE
    message: Generic untrusted-data marker (test.Taint.source)
    metadata:
      short-description: Generic test taint source
    languages:
      - java
    patterns:
      - pattern: |
          $UNTRUSTED = test.Taint.source();
