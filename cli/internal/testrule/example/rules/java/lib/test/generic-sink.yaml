rules:
  - id: generic-taint-sink
    options:
      lib: true
    severity: NOTE
    message: Generic dangerous-operation marker (test.Taint.sink)
    metadata:
      short-description: Generic test taint sink
    languages:
      - java
    patterns:
      - pattern: test.Taint.sink($VALUE)
      - focus-metavariable: $VALUE
