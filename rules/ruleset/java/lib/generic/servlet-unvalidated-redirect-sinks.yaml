rules:
  - id: java-servlet-unvalidated-redirect-sinks
    options:
      lib: true
    tags:
      - unvalidated-redirect-sink
    severity: NOTE
    message: Potential redirect to an untrusted URL
    metadata:
      provenance: https://gitlab.com/gitlab-org/security-products/sast-rules/-/blob/main/java/endpoint/rule-UnvalidatedRedirect.yml
    languages:
      - java
    mode: taint
    pattern-sinks:
      - patterns:
          - pattern: (HttpServletResponse $RES).sendRedirect($UNTRUSTED);
          - focus-metavariable: $UNTRUSTED
      - patterns:
          - pattern: (HttpServletResponse $RES).addHeader("Location", $UNTRUSTED);
          - focus-metavariable: $UNTRUSTED
    pattern-sanitizers:
      - patterns:
        - pattern: $UNTRUSTED = (HttpServletRequest $REQ).getContextPath();
        - focus-metavariable: $UNTRUSTED
