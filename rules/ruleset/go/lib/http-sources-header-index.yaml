rules:
  - id: go-http-sources-header-index
    options: {lib: true}
    languages: [go]
    severity: NOTE
    message: Untrusted header-map source
    mode: taint
    pattern-sources:
      - label: "$UNTRUSTED"
        pattern: $X.Header($K)
