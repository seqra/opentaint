rules:
  - id: ssrf-sinks
    options:
      lib: true
    severity: NOTE
    message: Untrusted data reaches an outbound HTTP request API
    metadata:
      short-description: Outbound HTTP request sink
      provenance: https://github.com/seqra/opentaint/tree/main/skills
    languages:
      - go
    mode: taint
    pattern-sinks:
      - patterns:
          - pattern-inside: |
              import "net/http"
              ...
          - pattern-either:
              - pattern: "http.Get($UNTRUSTED)"
              - pattern: "http.Head($UNTRUSTED)"
              - pattern: "http.Post($UNTRUSTED, ...)"
              - pattern: "http.PostForm($UNTRUSTED, ...)"
              - pattern: "($C : *http.Client).Get($UNTRUSTED)"
              - pattern: "($C : *http.Client).Head($UNTRUSTED)"
              - pattern: "($C : *http.Client).Post($UNTRUSTED, ...)"
              - pattern: "($C : *http.Client).PostForm($UNTRUSTED, ...)"
              - pattern: "http.NewRequest($M, $UNTRUSTED, ...)"
              - pattern: "http.NewRequestWithContext($CTX, $M, $UNTRUSTED, ...)"
          - focus-metavariable: $UNTRUSTED
