rules:
  - id: path-traversal
    languages: [go]
    severity: ERROR
    message: Tainted user input reaches filesystem path (path traversal)
    metadata:
      cwe: CWE-22
      short-description: Path traversal
    mode: join
    join:
      refs:
        - rule: go/lib/http-sources.yaml#http-sources
          as: src
        - rule: go/lib/path-sinks.yaml#path-sinks
          as: sink
      on:
        - 'src.$UNTRUSTED -> sink.$UNTRUSTED'
