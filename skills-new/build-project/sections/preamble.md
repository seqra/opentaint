# Skill: Build Project

Turn a target project into an opentaint project model: run the project's build, resolve its dependencies if needed. The build tool, toolchain, and scope flags are language-specific — read the `references/<language>.md` per Inputs and follow its numbered steps, which key to the ones below.
